# Claude handoff → P16 laptop (2026-08-22)

**Purpose:** let a Claude Code session on the P16 continue AEO Analyzers where the
other machine left off. Read this first, then `STATUS.md`, then `git log --oneline -15`.

> Written from the "other" (non-P16) machine, which had **flaky GitHub TLS** this
> session (pushes took retries; `gh api` timed out). The P16 should have clean
> connectivity — good, because the last mile (preview URL + external-fetch proof)
> needs it.

---

## 1. Orient (do this first)

```
git fetch --all --prune
git branch --show-current
git status --short
git log --oneline -15
```

Expected on arrival: origin has 3 branches — `main`, `gtm-drafts-aug13`,
`fix/mobile-history-aug22`. All were `0/0` (fully synced) as of this handoff.
Everything below is already committed + pushed; nothing is stranded locally.

---

## 2. ACTIVE WORK — WO-AEO-MOBILE-HISTORY-001 (branch `fix/mobile-history-aug22`)

Filed 2026-08-22 (founder). WO text: `docs/wo/WO-AEO-MOBILE-HISTORY-001.md`.
Branch is off `main`. Both parts are **implemented, verified, committed, pushed.**

Commits (on the branch, on top of `main`/`e1f3b15`):
- `1d5dc6d` — file WO in docs/wo/
- `6cd1e69` — **Part A** fix
- `9f49426` — **Part B** fix  ← branch tip

Verification (ran clean here): `npx tsc --noEmit` clean · `npm test` **206 pass** ·
`npm run build` clean (prerender fail-open locally — that's expected off-Vercel).

### Part A — roadmap tab/download clip on iPhone (SHIP NOW, freeze-compliant)
- Fixed `src/components/ImplementationRoadmap.tsx`: the header packed the 3 tabs
  (Summary/Handoff/Platforms) + Download Report in one non-wrapping flex row that
  overflowed below ~660px. Now below `md`: tabs wrap (all visible) and **Download
  is full-width below the tab row**; desktop unchanged.
- A3 audit of other horizontal rows at 375px → roadmap was the ONLY clip.
  Score-formula card (`grid-cols-2 md:grid-cols-4`), History tabs, sweep
  results/setup headers (`flex-wrap`), pricing cards (`flex-col md:flex-row`) were
  already mobile-safe. No other changes.
- **Gate to close:** real iPhone screenshots (tabs reachable + Download tappable),
  desktop 1280px pixel-diff = no change. Then merge Part A to `main`:
  ```
  git checkout main && git cherry-pick 1d5dc6d 6cd1e69
  ```
  (Part A and Part B are separate commits, so this lands Part A WITHOUT Part B.)

### Part B — Citation Sweep history parity (HOLD ON BRANCH until founder go-word)
- `src/App.tsx` + `src/components/SweepDashboard.tsx`: every sweep-history row is
  now a **View →** link → `/sweeps?saved=<id>`, rebuilding the identical results
  page from STORED data (`citation_sweeps` + `sweep_results`), **no engine calls,
  $0**. "Viewing Saved Sweep · {date}" banner + Back to History. Download (B2) and
  Transcripts "Show details" (B3) work from the saved view.
- **No migration, no new table** (B5). Migration status: **NONE**.
- **B4 — NOT rebuildable from stored data (not fabricated, not re-queried):**
  per-run `grounding`/`truncated`/`domainCited` are not persisted; and
  `fidelity`/`entityLinking`/content-depth(E2) are derived at run time from a LIVE
  site fetch (point-in-time) so those sections don't render in a saved view. Full
  parity later would need a forward-only `citation_sweeps.full_result jsonb`
  snapshot — but it would NOT backfill the existing Dolphinpools 8/21 sweep, so
  reconstruction-from-tables is correct for the acceptance case.
- **Gate to close:** founder go-word. Acceptance = open the **Dolphinpools.us
  8/21/2026** sweep from History; sections match the original; Download works;
  the History row cost stays **$0.45** (proves no re-run). Then merge Part B.

### The LAST MILE (blocked here, unblock on P16)
Could not fetch the Vercel preview from the other machine: Vercel CLI had no auth
token, Vercel MCP was disconnected, GitHub API TLS-timed-out. On P16:
1. Get the preview URL: Vercel dashboard → project `aeo-app1` → Deployments →
   branch `fix/mobile-history-aug22` (or the "vercel" check on commit `9f49426`).
   CLI alt (if authed): `vercel ls aeo-app1`.
2. Record **deploy id + commit hash (`9f49426`) + external-fetch proof** per the
   standing GTM rule (no ✅ without it).
3. Device-test Part A (iPhone) and Part B (saved-sweep view) → founder screenshots.

---

## 3. Broader project state (so you don't re-derive it)

- **Release v1.8.x · FEATURE FREEZE / maintenance mode** (GTM-90 90-day window):
  bug fixes + paying-customer blockers only. This WO qualifies (Part A = bug;
  Part B = founder-requested customer value, held behind go-word). Freeze +
  backlog: `docs/FREEZE-REPORT-2026-07-31.md`. Authoritative GTM plan:
  `docs/GTM90-STATUS-AND-PLAN-2026-08-05.md`.
- **Next major build (do NOT start without founder word):** WO-AEO-EXECREPORT-001
  ($250 AI Visibility Executive Report — 5 phases, human-review-gated both doors).
  Template seeded in `templates/exec-report/`. Sequenced after dogfooding.
- **Unpublished drafts on `gtm-drafts-aug13`** (5 commits ahead of `main`, all
  noindexed, NOT on `main`): D2 `/evidence`, D3 `/methodology`, G4 `/about`, blog
  Part 3 (invented-co-founder), two Sept-1 pre-writes. These were built for a
  founder review targeted Aug 14–16 → publish before Aug 19. **Today is past that
  window — needs a founder go/hold decision** before publishing.
- **Google GCP incident (lesson #10):** resolved/hardened portfolio-wide; the
  reinstatement-appeal reply was SENT (ticket `3A557AW7MTCPSUR73FS24EXI2Y`) +
  fraud-credit case `73674480` — both awaiting Google. Full report lives in the
  `pigenai-website` repo (`docs/GCP-INCIDENT-MITIGATION-2026-07-24-25.md`), NOT
  this repo. AEO's part (server-side key move, sweep-engine-fail flag) shipped.

## 4. Env / local dev on P16

- `.env` is present here and is a **superset** of `.env.example` (adds
  `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `PERPLEXITY_API_KEY`, `ADMIN_SWEEP_TOKEN`
  for the 4-engine sweep). Confirm the P16's `.env` matches names key-for-key.
- `.env.local` does NOT exist here (Vite reads `.env`). If the P16 uses
  `.env.local`, that's fine — just don't expect it from this machine.
- `VITE_STRIPE_PRICE_ID_REPORT` (the $24 Day Pass price, per STATUS) is in neither
  local env file — pull it from Vercel/P16 if testing Day Pass locally.

## 5. Guardrails (standing)

- Repo is **PUBLIC** — never commit prospect data, secrets, emails, or the
  `private/` slate. Secrets are env-only (`$ENV_VAR` in allow-rules).
- Do NOT merge Part B until founder go-word. Do NOT publish the `gtm-drafts-aug13`
  pages without founder review. Do NOT start the Exec Report build unprompted.
- Acceptance = observation (real-device screenshots / external fetch / probe),
  never attestation. No copy claims a feature before its deploy id is recorded.
- Models: pin FAMILY aliases (`claude-opus-4-8`, gemini flash chain) — never dated
  snapshots.
